
mydb {
  driver = "slick.jdbc.H2Profile$"
  db {
    profile = org.h2.Driver
    url = "jdbc:h2:mem:sample;INIT=RUNSCRIPT FROM 'sql/drop-tables.sql'\\;RUNSCRIPT FROM 'sql/create-tables.sql'\\;RUNSCRIPT FROM 'sql/populate-tables.sql'"
    user = sa
    password = ''
  }
}
